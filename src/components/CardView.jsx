import React from'react';import{cat,fmt,META,summary}from'../calendar';const order=['nonfinal','final','restriction','interview','m3.5y','m7.5y','m11.5y'];export default function CardView({events,onOpen}){const g={};events.filter(e=>e.type!=='holiday'&&cat(e)!=='other').forEach(e=>(g[cat(e)]||=[]).push(e));return <section className="cards">{order.filter(k=>g[k]?.length).map(k=><article className="deadlineCard" key={k}><header className={META[k][1]}><b>{META[k][0]}</b><span>{g[k].length} deadlines</span></header><div>{g[k].sort((a,b)=>a.due_date.localeCompare(b.due_date)).map(e=><button className={META[k][1]} onClick={()=>onOpen(e)} key={e.id}><b>{fmt(e.due_date)}</b><strong>{summary(e)}</strong><span>{(e.inventor_list||'Unknown inventor').split(/[;,]/)[0]}</span><small>{e.title}</small>{e.is_estimated&&<em>Estimated</em>}</button>)}</div></article>)}</section>}