import fs from'node:fs';const p='public/data/deadlines.json';const d=JSON.parse(fs.readFileSync(p,'utf8'));if(!d.organization_name||!Array.isArray(d.events))throw new Error('Invalid deadline snapshot.');for(const e of d.events){if(!e.id||!e.type||!/^\d{4}-\d{2}-\d{2}$/.test(e.due_date))throw new Error('Invalid event: '+JSON.stringify(e));}console.log('Validated '+d.events.length+' events.');
